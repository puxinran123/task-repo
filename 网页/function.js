// javascript一直弄不好，我后面就没有用这个了
/*
var articles = [
  {title: '文章1', content: '这是第一篇文章的内容'},
  {title: '文章2', content: '这是第二篇文章的内容'},
  {title: '文章3', content: '这是第三篇文章的内容'},
  // 可以添加更多的文章
];

// 将文章添加到页面上
articles.forEach(function(article) {
  var div = document.createElement('div');
  div.className = 'article';
  div.innerHTML = '<h2>' + article.title + '</h2><p>' + article.content + '</p>';
  document.body.appendChild(div);
});
*/
// 添加搜索功能
/*document.getElementById('searchButton').addEventListener('click', function search() {
  var keyword = document.getElementById('searchBox').value;
  var results = document.getElementsByClassName('article');
  
  for (var i = 0; i < results.length; i++) {
      if (results[i].textContent.includes(keyword)) {
          results[i].style.display = 'block';
      } else {
          results[i].style.display = 'none';
      }
  }
});*/

